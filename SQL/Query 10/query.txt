SELECT TOP 1 station.CITY, LEN(station.CITY)
FROM STATION AS station
ORDER BY LEN(CITY), station.CITY;

SELECT TOP 1 station.CITY, LEN(station.CITY)
FROM STATION AS station
ORDER BY LEN(CITY) DESC, station.CITY;
